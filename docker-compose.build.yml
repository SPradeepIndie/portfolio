# Docker Compose file for building images locally (Development)
# Usage: docker-compose -f docker-compose.yml -f docker-compose.build.yml up -d

version: '3.8'

services:
  backend:
    image: ${DOCKER_USERNAME:-yourusername}/portfolio-backend:${TAG:-latest}
    build:
      context: ./backend
      dockerfile: Dockerfile

  frontend:
    image: ${DOCKER_USERNAME:-yourusername}/portfolio-frontend:${TAG:-latest}
    build:
      context: ./frontend
      dockerfile: Dockerfile
