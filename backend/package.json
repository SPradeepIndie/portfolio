{
  "name": "portfolio-backend",
  "version": "1.0.0",
  "description": "Node.js Express backend for portfolio website",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "check": "find . -name '*.js' -not -path './node_modules/*' -print0 | xargs -0 -n1 node --check",
    "test": "npm run check",
    "migrate:up": "db-migrate up --env dev",
    "migrate:up:prod": "db-migrate up --env prod",
    "migrate:down": "db-migrate down --env dev",
    "migrate:reset": "db-migrate reset --env dev",
    "migrate:status": "db-migrate check --env dev",
    "migrate:create": "db-migrate create --env dev",
    "substitute:env": "node substitute-env.js"
  },
  "keywords": [
    "portfolio",
    "express",
    "api"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@azure/storage-blob": "^12.24.0",
    "bcryptjs": "^3.0.3",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.3",
    "morgan": "^1.10.1",
    "multer": "^2.0.2",
    "pg": "^8.17.1",
    "swagger-jsdoc": "^6.2.8",
    "swagger-ui-express": "^5.0.1"
  },
  "devDependencies": {
    "db-migrate": "^0.11.14",
    "db-migrate-pg": "^1.5.2",
    "nodemon": "^3.1.10"
  }
}
